// @ts-nocheck
// TODO(story): cloud removed - Story app renders directly without workspace
import { StoryLayout } from '@affine/core/components/story-layout';
import type { ReactElement } from 'react';

export const Component = (): ReactElement => {
  return <StoryLayout />;
};
